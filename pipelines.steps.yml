pipelines:
  - name: basic1
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: host
          inputResources:
           - name: git
          environmentVariables:
            foo: bar
        execution:
          onExecute:
            - printenv
            - echo lol
            - add_pipeline_variables foo=bar
